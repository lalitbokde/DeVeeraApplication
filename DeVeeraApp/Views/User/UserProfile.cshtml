@model DeVeeraApp.ViewModels.User.UserModel
@using DeVeeraApp.ViewModels.User
@inject CRM.Core.IWorkContext _workContext

@{
    ViewData["Title"] = "UserProfile";

    var dataProperty = _workContext.CurrentUser.UserRole.Name == "Admin" ? "disabled" : "";
}


<div class="intro-y flex items-center mt-8">
    <h2 class="text-lg font-medium mr-auto">
        Update Profile
    </h2>
</div>
<div class="grid grid-cols-12 gap-6">
    <!-- BEGIN: Profile Menu -->
    <div class="col-span-12 lg:col-span-4 xxl:col-span-3 flex lg:block flex-col-reverse">
        <div class="intro-y box mt-5">
            <div class="relative flex items-center p-5">
                <div class="w-12 h-12 image-fit">
                    <img alt="Icewall Tailwind HTML Admin Template" class="rounded-full" src="~/dist/images/profile-15.jpg">
                </div>
                <div class="ml-4 mr-auto">
                    <div class="font-medium text-base">@Model.Email</div>
                    <div class="text-gray-600">@Model.UserRoleName</div>
                </div>
            </div>
            <div class="p-5 border-t border-gray-200 dark:border-dark-5">
                <a class="flex items-center text-theme-17 dark:text-gray-300 font-medium" data-toggle="tab" data-target="#accountInfo"> <i data-feather="activity" class="w-4 h-4 mr-2"></i> Personal Information </a>
                <a class="flex items-center mt-5" data-toggle="tab" data-target="#changePassword"> <i data-feather="lock" class="w-4 h-4 mr-2"></i> Change Password </a>
                <a class="flex items-center mt-5"> <i data-feather="settings" class="w-4 h-4 mr-2"></i> User Settings </a>
                <a asp-controller="Home" asp-action="Help" class="flex items-center mt-5"> <i data-feather="help-circle" class="w-4 h-4 mr-2"></i> Help </a>
            </div>
        </div>
        @if (_workContext.CurrentUser.UserRole.Name == "Admin")
        {
            @if (Model.LevelTitle != null)
            {
                <div class="intro-y box p-5 bg-theme-31  text-theme-26 mt-5">
                    <div class="flex items-center">
                        <div class="font-medium text-lg">@Model.LevelTitle</div>
                        <div class="text-xs bg-white dark:bg-theme-17 dark:text-white text-gray-800 px-1 rounded-md ml-auto">Last Level</div>
                    </div>
                 
                </div>

            }
            @if (Model.ModuleTitle != null)
            {
                <div class="intro-y box p-5 bg-theme-17 text-white mt-5">
                    <div class="flex items-center">
                        <div class="font-medium text-lg">@Model.ActiveModuleLevelName: @Model.ModuleTitle</div>
                        <div class="text-xs bg-white dark:bg-theme-17 dark:text-white text-gray-800 px-1 rounded-md ml-auto">Active Module</div>
                    </div>
                  
                </div>

            }

        }
    </div>
    <div class="col-span-12 lg:col-span-8 xxl:col-span-9">

        <div class="tab-content">

            <div id="changePassword" class="tab-pane" role="tabpanel" aria-labelledby="profile-tab">
                <form asp-controller="User" asp-action="UserProfile" method="post">
                    <input asp-for="Id" hidden />
                    <input asp-for="@Model.Email" hidden   />
                    <div class="intro-y box lg:mt-5">
                        <div class="flex items-center p-5 border-b border-gray-200 dark:border-dark-5">
                            <h2 class="font-medium text-base mr-auto">
                                Change Password
                            </h2>
                        </div>
                        <div class="p-5">
                            <div class="flex flex-col-reverse xl:flex-row flex-col">
                                <div class="flex-1 mt-6 xl:mt-0">
                                    <div class="grid grid-cols-12 gap-x-5">
                                        <div class="col-span-12 xxl:col-span-6">
                                            <div>
                                                <label class="form-label">New Password</label>
                                                <input asp-for="@Model.UserPassword.Password" type="password" class="form-control" placeholder="Password">
                                                <span asp-validation-for="UserPassword.Password" class="text-theme-6 mt-2"></span>

                                            </div>
                                            <div class="mt-3">
                                                <label class="form-label">Confirm Password</label>
                                                <input asp-for="@Model.ConfirmPassword" type="password" class="form-control" placeholder="Confirm Password">
                                                <span asp-validation-for="ConfirmPassword" class="text-theme-6 mt-2"></span>

                                            </div>
                                        </div>
                                    </div>
                                   
                                    <button type="submit" class="btn btn-primary w-20 mt-3" onclick="LoadLoader()">Save</button>
                                </div>
                            </div>
                        </div>
                    </div>

                </form>
            </div>
            <div id="accountInfo" class="tab-pane active" role="tabpanel" aria-labelledby="profile-tab">
                <form asp-controller="User" asp-action="UserProfile" method="post">
                    <!-- BEGIN: Display Information -->
                    <div class="intro-y box lg:mt-5">
                        <div class="flex items-center p-5 border-b border-gray-200 dark:border-dark-5">
                            <h2 class="font-medium text-base mr-auto">
                                Display Information
                            </h2>
                        </div>
                        <div class="p-5">
                            @if (ViewData.ModelState.Any(x => x.Value.Errors.Any()))
                            {
                                // Bootstrap 2 = "alert-error", Bootstrap 3 and 4 = "alert-danger"
                                <div asp-validation-summary="All" class="text-theme-6 mt-4">
                                    @*<a class="close" data-dismiss="alert">&times;</a>*@
                                </div>
                            }
                            <div class="flex flex-col-reverse xl:flex-row flex-col">
                                <div class="flex-1 mt-6 xl:mt-0">
                                    <div class="grid grid-cols-12 gap-x-5">
                                        <div class="col-span-12 xxl:col-span-6">
                                            <div>
                                                <label class="form-label">User Name</label>
                                                <input asp-for="Id" hidden />
                                                <input asp-for="Email" type="text" class="form-control" placeholder="User Name" readonly>
                                                <span asp-validation-for="Email" class="text-theme-6 mt-2"></span>

                                            </div>
                                            <div class="mt-3">
                                                <label class="form-label">Gender</label>
                                                <select asp-for="@Model.GenderType" asp-items="@Html.GetEnumSelectList<Gender>()" class="tail-select w-full">
                                                </select>
                                                <span asp-validation-for="GenderType" class="text-theme-6 mt-2"></span>

                                            </div>
                                        </div>
                                        <div class="col-span-12 xxl:col-span-6">
                                            <div class="mt-3 xxl:mt-0">
                                                <label class="form-label">Age</label>
                                                <input asp-for="Age" type="number" class="form-control" placeholder="your age">
                                                <span asp-validation-for="Age" class="text-theme-6 mt-2"></span>

                                            </div>
                                            <div class="mt-3">
                                                <label class="form-label">Education</label>
                                                <select asp-for="@Model.EducationType" asp-items="@Html.GetEnumSelectList<Education>()" class="tail-select w-full">
                                                </select>
                                            </div>
                                        </div>
                                        <div class="col-span-12">
                                            <div class="mt-3">
                                                <label class="form-label">Occupation</label>
                                                <input asp-for="Occupation" class="form-control" placeholder="your occupation" />

                                            </div>
                                            <div class="mt-3">
                                                <label class="form-label">Family Or Relationship</label>
                                                <select asp-for="@Model.FamilyOrRelationshipType" asp-items="@Html.GetEnumSelectList<FamilyOrRelationship>()" class="tail-select w-full">
                                                </select>
                                            </div>
                                        </div>
                                    </div>
                                    @*<span class="btn btn-outline-secondary relative justify-end rounded-full h-10 w-32">
                                        Back to List
                                        <a class="w-8 h-8 absolute flex justify-center items-center bg-theme-17 dark:bg-theme-26 text-white rounded-full  top-0 bottom-0 my-auto pl-24 mr-padding " asp-controller="Admin" asp-action="List" asp-route-roleName="@(_workContext.CurrentUser.UserRole.Name == "Admin" ? "User" : "User")">
                                            <i data-feather="arrow-left" class="w-4 h-4"></i>
                                        </a>
                                    </span>*@
                                    <button type="submit" class="btn btn-primary w-20 mt-3" onclick="LoadLoader()">Save</button>
                                </div>

                            </div>
                        </div>
                    </div>
                    <!-- END: Display Information -->

                </form>


            </div>

        </div>

    </div>
</div>
<!-- END: Profile Menu -->
