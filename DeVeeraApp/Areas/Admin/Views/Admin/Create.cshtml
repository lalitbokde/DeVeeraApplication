@model DeVeeraApp.ViewModels.Admin.CreateAdminModel
@inject CRM.Core.IWorkContext _workContext;
@using DeVeeraApp.ViewModels.User
@{
    ViewData["Title"] = "Create";
}
<form asp-controller="Admin" asp-action="Create" method="post"  id="adminPage" onsubmit="ShowLoader('adminPage');">
    <div class="intro-y  py-10 sm:py-20 ">

        <div class="px-5 ">
            <div class="font-medium text-center text-lg">Create Admin</div>
            <div class="text-gray-600 text-center mt-2">Here you can create new admin user.</div>

        </div>


        <div class="px-5 sm:px-20 pt-5 border-t border-gray-200 dark:border-dark-5">

            <div class="grid grid-cols-12 gap-4 gap-y-5 mt-5">
                <div class="intro-y col-span-3"></div>
                <div class="intro-y box p-5 col-span-6">
                    @if (ViewData.ModelState.Any(x => x.Value.Errors.Any()))
                    {
                        <div asp-validation-summary="All" class="text-theme-6 mt-4">
                        </div>
                    }
                    <div class="mt-5">
                        <label for="input-wizard-1" class="form-label text-theme-21 star">User Name</label>
                        <input id="email" asp-for="@Model.Email" type="email" class="form-control" placeholder="example@gmail.com" autocomplete="new-username" >
                        <span asp-validation-for="Email" class="text-theme-21 mt-2"></span>
                    </div>
                    <div class="mt-5">
                        <label for="input-wizard-3" class="form-label text-theme-21 star">Password</label>
                        <input id="password" asp-for="@Model.UserPassword.Password" type="password" class="form-control" placeholder="password" autocomplete="new-password" >
                        <span asp-validation-for="UserPassword.Password" class="text-theme-21 mt-2"></span>
                    </div>

                    <div class="intro-y col-span-12 flex items-center justify-center sm:justify-end mt-5">
                        <div class="grid grid-cols-12  gap-y-5 mt-5">
                            <div class="intro-y   col-span-6">   
                                @*<span class="pl-0" >
                                    <a class="w-8 h-8 absolute flex justify-center items-center btn-primary dark:bg-theme-26 text-white rounded-full  top-0 bottom-0 my-auto pl-24 mr-padding " asp-controller="Admin" asp-action="List" asp-route-roleName="@_workContext.CurrentUser.UserRole.Name">
                                        <i data-feather="arrow-left" class="w-4 h-4"></i>
                                    </a>
                                </span>*@
                                @*<a asp-controller="Admin" asp-action="List" asp-route-roleName="@_workContext.CurrentUser.UserRole.Name" class="btn btn-primary w-24"> List</a>*@
                                @*<span class="btn btn-outline-secondary relative justify-end rounded-full h-10 w-32">
                               Back to List

                                </span>*@
                                <span class="btn btn-outline-secondary relative justify-start rounded-full h-10 w-40">
                                    <label class="pl-10">Back to List</label>
                                    <a class="w-8 h-8 absolute flex justify-center items-center bg-theme-17 dark:bg-theme-26 text-white rounded-full  top-0 bottom-0 my-auto pl-24 mr-padding " asp-controller="Admin" asp-action="List" asp-route-roleName="@_workContext.CurrentUser.UserRole.Name">
                                        <i data-feather="arrow-left" class="w-4 h-4"></i>
                                    </a>
                                </span>
                            </div>
                            <div class="intro-y  col-span-6 ml-auto">
                                <button type="submit" class="btn btn-primary w-24" >Save</button>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="intro-y col-span-3"></div>

            </div>

        </div>
    </div>

</form>


